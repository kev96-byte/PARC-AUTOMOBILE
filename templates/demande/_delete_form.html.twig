<form method="post" action="{{ path('demande.delete', {'id': demande.id}) }}" onsubmit="return confirm('Voulez-vous vraiment effectuer cette suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ demande.id) }}">
    <button class="btn">Delete</button>
</form>
