<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Afficher liste des véhicules et chauffeurs d'une mission</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
    <script src="{{ asset('js/jquery.min.js') }}"></script>
    <script src="{{ asset('js/bootstrap.min.js') }}"></script>
    <style>
        @media print {
            body {
                margin: 0;
                padding: 0;
            }
            .print-footer {
                display: none;
            }
        }

        .logo-container img {
            max-width: 150px;
            height: auto;
        }

        .info-container {
            text-align: right;
        }

        .signature-space {
            height: 50px;
            margin-bottom: 30px;
        }

        .info-container p {
            margin: 0;
        }

        .action-buttons {
            display: flex;
            justify-content: flex-end;
            margin-bottom: 20px;
        }

        .action-buttons .btn {
            margin-left: 10px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th, td {
            padding: 8px;
            border: 1px solid #ddd;
        }

        th {
            background-color: #f2f2f2;
        }

        .text-center {
            text-align: center;
        }

        .text-right {
            text-align: right;
        }

        footer {
            text-align: left;
            margin-top: 30px;
            padding-top: 10px;
            border-top: 1px solid #ddd;
            font-size: 0.5em;
        }

        .footer-content {
            font-weight: bold;
        }

        .footer-content span {
            font-weight: normal;
        }
    </style>
</head>
<body>
    <header class="container mt-4 mb-4">
        <div class="d-flex justify-content-between align-items-center">
            <img class="img-fluid" src="{{ asset('img/entete.jpg') }}" alt="Logo">
            <div class="action-buttons">
                <button class="btn btn-primary" onclick="window.print()">
                    <i class="fa fa-print"></i>
                </button>
                <a href="{{ path('demande_pdf', {'id': demande.id}) }}" class="btn btn-secondary">
                    <i class="fa fa-download"></i>
                </a>
            </div>
        </div>
        <hr>
    </header>
    <main class="container">
        <p class="text-right"><strong>COTONOU, le {{ "now" | date("d/m/Y") }}</strong></p>

        <h2 class="text-center">Direction de la Planification de l'Administration et des Finances (DPAF)</h2>
        <h4 class="text-center mb-4">LISTE DES VÉHICULES ET CHAUFFEURS AFFECTÉS À LA DEMANDE N° {{ demande.numDemande }}</h4>

        <table class="table">
            <tr>
                <td colspan="2"><h4 class="text-center">Informations de la mission</h4></td>
            </tr>
            <tr>
                <td><h6>Bénéficiaire :</h6></td>
                <td>{{ demande.structure.libelleStructure }}</td>
            </tr>
            <tr>
                <td><h6>Point focal :</h6></td>
                <td>{{ demande.demander.lastName }} {{ demande.demander.firstName }}</td>
            </tr>
            <tr>
                <td><h6>Responsable structure :</h6></td>
                <td>{{ demande.structure.responsableStructure.lastName }} {{ demande.structure.responsableStructure.firstName }}</td>
            </tr>
            <tr>
                <td><h6>Chef parc :</h6></td>
                <td>{{ demande.structure.parc.chefParc.lastName }} {{ demande.structure.parc.chefParc.firstName }}</td>
            </tr>
            <tr>
                <td><h6>Référence document d'autorisation :</h6></td>
                <td>{{ demande.referenceNoteDeService }} du {{ demande.dateSignatureNoteDeService | date('d/m/Y') }}</td>
            </tr>
            <tr>
                <td><h6>Objet :</h6></td>
                <td>{{ demande.objetMission }}</td>
            </tr>
            <tr>
                <td><h6>Lieu :</h6></td>
                <td>
                    {% for value in demande.lieuMission %}
                        <strong>{{ value | upper }}</strong>
                        {% if not loop.last %}
                            <strong>* *</strong>
                        {% endif %}
                    {% endfor %}
                </td>
            </tr>
            <tr>
                <td><h6>Date début :</h6></td>
                <td>{{ demande.dateDebutMission | date('d/m/Y') }}</td>
            </tr>
            <tr>
                <td><h6>Date fin :</h6></td>
                <td>{{ demande.dateFinMission | date('d/m/Y') }}</td>
            </tr>
        </table>

        <table class="table table-bordered mt-4">
            <thead>
                <tr>
                    <th colspan="4" class="text-center" style="border-top: none; border-left: none; border-right: none;">
                        Détails sur les véhicules et chauffeurs affectés
                    </th>
                </tr>
                <tr style="background-color: darkgray;">
                    <th>Matricule</th>
                    <th>Nombre de km restant à parcourir</th>
                    <th>Portée</th>
                    <th>Chauffeur</th>
                </tr>
            </thead>
            <tbody>
                {% for affectation in affectations %}
                    {% set vehicule = affectation.vehicule %}
                    {% set chauffeur = affectation.chauffeur %}
                    {% set nbrekmrestant = vehicule.nbreKmPourRenouvellerVidange - (vehicule.kilometrageCourant - vehicule.kilometrageInitial) %}
                    <tr>
                        <td>{{ vehicule.matricule }}</td>
                        <td>{{ nbrekmrestant }}</td>
                        <td>{{ vehicule.porteeVehicule }}</td>
                        <td>{{ chauffeur.nomChauffeur }} {{ chauffeur.prenomChauffeur }}</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </main>

    <footer>
        PARC AUTO : <span class="footer-content"> {{ demande.numDemande }} du {{ demande.dateDemande | date('d/m/Y') }}</span>
    </footer>
</body>
</html>
