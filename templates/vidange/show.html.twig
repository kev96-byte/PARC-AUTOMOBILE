{% extends 'base.html.twig' %}

{% block title %}Vidange{% endblock %}

{% block body %}
    <h1>Vidange</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ vidange.id }}</td>
            </tr>
            <tr>
                <th>DateVidange</th>
                <td>{{ vidange.dateVidange ? vidange.dateVidange|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>ValeurCompteurKilometrage</th>
                <td>{{ vidange.valeurCompteurKilometrage }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_vidange_index') }}">back to list</a>

    <a href="{{ path('app_vidange_edit', {'id': vidange.id}) }}">edit</a>

    {{ include('vidange/_delete_form.html.twig') }}
{% endblock %}
