<form method="post" action="{{ path('vidange.delete', {'id': vidange.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vidange.id) }}">
    <button class="btn">Delete</button>
</form>
