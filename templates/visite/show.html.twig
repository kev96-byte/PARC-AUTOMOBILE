{% extends 'base.html.twig' %}

{% block title %}Visite{% endblock %}

{% block body %}
    <h1>Visite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ visite.id }}</td>
            </tr>
            <tr>
                <th>DateDebutVisite</th>
                <td>{{ visite.dateDebutVisite ? visite.dateDebutVisite|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFinVisite</th>
                <td>{{ visite.dateFinVisite ? visite.dateFinVisite|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PieceVisite</th>
                <td>{{ visite.pieceVisite }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_visite_index') }}">back to list</a>

    <a href="{{ path('app_visite_edit', {'id': visite.id}) }}">edit</a>

    {{ include('visite/_delete_form.html.twig') }}
{% endblock %}
