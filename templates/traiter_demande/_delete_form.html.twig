<form method="post" action="{{ path('app_traiter_demande_delete', {'id': traiter_demande.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ traiter_demande.id) }}">
    <button class="btn">Delete</button>
</form>
