{% extends 'base.html.twig' %}

{% block title %}TypeVehicule{% endblock %}

{% block body %}
    <h1>TypeVehicule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_vehicule.id }}</td>
            </tr>
            <tr>
                <th>LibelleTypeVehicule</th>
                <td>{{ type_vehicule.libelleTypeVehicule }}</td>
            </tr>
            <tr>
                <th>DeleteAt</th>
                <td>{{ type_vehicule.deleteAt ? type_vehicule.deleteAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_vehicule_index') }}">back to list</a>

    <a href="{{ path('app_type_vehicule_edit', {'id': type_vehicule.id}) }}">edit</a>

    {{ include('type_vehicule/_delete_form.html.twig') }}
{% endblock %}
