{% extends 'base.html.twig' %}

{% block title %}Parc{% endblock %}

{% block body %}
    <h1>Parc</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ parc.id }}</td>
            </tr>
            <tr>
                <th>NomParc</th>
                <td>{{ parc.nomParc }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_parc_index') }}">back to list</a>

    <a href="{{ path('app_parc_edit', {'id': parc.id}) }}">edit</a>

    {{ include('parc/_delete_form.html.twig') }}
{% endblock %}
