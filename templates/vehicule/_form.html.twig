{{ form_start(form) }}

<div style="position: relative;" >

    <div class="form-group">
        {{ form_row(form.photoVehicule, {
            'attr': {'placeholder': 'Télécharger le fichier', 'class':'form-control form-control-user'}
        }) }}
    </div>

    <div class="form-group row bm-3">
        <div class="col-md-4">
            {{ form_row(form.Kilometrage, {
                
            }) }}
        </div>
        <div class="col-md-4">
            {{ form_row(form.valeurAcquisition, {
                
            }) }}
        </div>
        <div class="col-md-4">
            {{ form_row(form.matricule, {
                
            }) }}
        </div>
    </div>    



    
    <div style="position: absolute; bottom: 0; right: 0; top: 2020px;">
        {{ form_widget(form.save, {'label': (mode == 'add' ? 'Ajouter' : 'Modifier')}) }}
        {{ form_widget(form.cancel, {'attr': {'class': 'btn btn-primary', 'onclick': 'resetFormFields()'}}) }}
    </div>
</div>

{{ form_end(form) }}

